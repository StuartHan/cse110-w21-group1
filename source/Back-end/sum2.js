/** 
 * Adds two numbers (duh, really?)
 * @param {int} a - first number you want to add.
 * @param {int} b - second number you want to add
 */

function sum2(a, b) {
  return a + b;
}
module.exports = sum2;
